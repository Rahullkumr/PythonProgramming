# 1.wap to perform addition and subtraction if "a" is greater than "b" return sum else return difference
def sum_diff(a,b):
    if a > b:
        return a+b
    else:
        return a-b

a = 75
b = 10
print(sum_diff(a,b))